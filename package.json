{
  "name": "shockwave-website",
  "version": "1.0.0",
  "scripts": {
    "build": "node build-for-ghl.js",
    "watch": "nodemon -w sections/ -w shockwave-* -e css,js -x 'npm run build'",
    "dev": "npm run build && npm run watch"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
